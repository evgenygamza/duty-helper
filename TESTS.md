# Проверки

Что и как проверено. Пополняется по мере работы: сделали кусок — здесь
появляется проверка и её результат.

Всё прогоняется руками. Автоматических тестов нет: почти каждая проверка это
разговор с живым Slack и живой моделью. Превращать их в скрипт имеет смысл,
когда набор устоится.

Проверки 1-22 сделаны в прежней песочнице `sandbox-d879184`: каналы
`#channel-with-bot` `C0BSKTQ1M5G`, `#channel-without-bot` `C0BTSTZ2J3X`,
`#duty-feed` `C0BTQL7AK4K`, доска `F0BU23Y14PQ`.

С проверки 23 — песочница `duty-helper`, Enterprise-орг `E0BU1D8791B`,
воркспейс `T0BU98BAB5J`: `#channel-with-bot` `C0BU5TL051Q`, `#duty-feed`
`C0BU7RTJCDP`, доска `F0BU1P86005`, группа `S0BV0EVS7ME`.

## Сбор

| # | Что проверяем | Как | Ожидаем | Итог |
|---|---|---|---|---|
| 1 | призыв в канале с ботом ловится | тег группы в `#channel-with-bot` | карточка на доске | ✅ 01.09 |
| 2 | сообщение без тега игнорируется | обычный текст туда же | ничего | ✅ 01.09 |
| 3 | из канала без бота событий нет | тот же тег в `#channel-without-bot` | карточка **не** заводится | ✅ 01.09 |
| 4 | поиск такой призыв всё же находит | `search.messages` user-токеном | находит, задержка ~20 сек | ✅ 01.09 |
| 5 | тред читается там, где бота нет | `conversations.replies` user-токеном | все сообщения | ✅ 01.09 |

Пункты 3-5 вместе обосновывают Э5: события покрывают не всё, поиск покрывает,
но с задержкой.

## Карточка

| # | Что проверяем | Как | Ожидаем | Итог |
|---|---|---|---|---|
| 6 | тред читается целиком | призыв в тред с ответами | в «Данных» сущности из ответов, не только из первого сообщения | ✅ 01.09 |
| 7 | заголовок не подменяется просьбой | повторный призыв «поднимаю», «что там?» | в «Обращении» суть, а не «просят посмотреть» | ✅ 01.09 |
| 8 | срок проставляется | завести карточку | `Due Date` = день заведения | ✅ 01.09 |
| 9 | дубли не плодятся | тегнуть трижды в одном треде | карточка одна | ✅ 01.09 |
| 10 | карточка освежается | дописать в тред данные и позвать | «Данные» пополнились | ✅ 01.09 |
| 11 | правки человека не затираются | переписать «Проблему» вручную, позвать снова | человеческий текст на месте | ✅ 01.09 |

## Сабтаски

| # | Что проверяем | Как | Ожидаем | Итог |
|---|---|---|---|---|
| 12 | вторая проблема в треде → сабтаск | «и заодно, не связанное: …» | дочерняя карточка, родитель не размылся | ✅ 01.09, дважды |
| 13 | нетерпение сабтаск не создаёт | «ну что там?» | обновление, не дробление | ✅ 01.09 |

Слабое место: 12 и 13 проверены на моих формулировках, а они чище живых.
На реальных тредах нужен отдельный прогон — пункт висит в Э5.

## Отказы

| # | Что проверяем | Как | Ожидаем | Итог |
|---|---|---|---|---|
| 14 | падение не проглатывается | подменить модель на несуществующую | в `#duty-feed` «не смог завести карточку» со ссылкой | ✅ 01.09 |
| 15 | то же на настоящем сбое | упёрлись в лимит Gemini (20 запросов) | то же сообщение, обращение не потеряно | ✅ 01.09 |
| 16 | нет доступа к модели — бот не стартует | запуск без ключа | внятный отказ на старте | ✅ 01.09 |

## Обратная связь от доски

| # | Что проверяем | Как | Ожидаем | Итог |
|---|---|---|---|---|
| 17 | `file_change` на смену статуса карточки | человеком сменить статус при живом боте | событие в логе | ❌ 01.09, тишина |
| 18 | `file_change` на правку текста карточки | человеком переписать «Обращение» | событие в логе | ❌ 01.09, тишина |
| 19 | `file_change` на переименование списка | человеком добавить точку в название | событие в логе | ❌ 01.09, тишина |
| 20 | контроль: сокет вообще доставляет | обычное сообщение в `#channel-with-bot` | `Unhandled request` в логе | ✅ 01.09, два события |
| 21 | подписка зарегистрирована у Slack | `slack manifest info` | `file_change` в `bot_events` | ✅ 01.09 |
| 22 | у бота есть доступ к файлу списка | `files.info` | `access: owner`, шара в `#duty-feed` с `write` | ✅ 01.09 |

19 и 20 вместе делают вывод твёрдым: сообщения доехали за десять секунд
до переименования, само переименование подтверждено (`edit_timestamp`
16:38:11, заголовок сменился), события нет. **Slack Lists не рождают
файловых событий вовсе** — ни на элемент, ни на сам список.

Не проверено: дёргается ли `files.info.updated` на **чистую смену статуса**.
На правку текста карточки и на переименование списка дёргается.

## Переезд на Enterprise-песочницу

| # | Что проверяем | Как | Ожидаем | Итог |
|---|---|---|---|---|
| 23 | песочницу даёт только Developer Program | `slack sandbox create` до и после регистрации | до — отказ, после — создаётся | ✅ 01.09, `invalid_access` → создана |
| 24 | custom steps требуют платного тарифа | один манифест с `functions` на двух воркспейсах | Enterprise `Valid`, Free — отказ | ✅ 01.09, `free_team_not_allowed` на `/settings/function_runtime` |
| 25 | `functions` требуют двух полей | манифест без них | `function_runtime_required`, `org_deploy_required` | ✅ 01.09 |
| 26 | группу призывов бот создать не может | `usergroups.create` ботовым токеном | отказ, группу заводит человек | ✅ 01.09, `permission_denied` |
| 27 | схема колонок читается на старте | поднять бота | в логе `board F0BU1P86005: 13 columns` | ✅ 01.09 |
| 28 | призыв заводит карточку на новой доске | тег группы в `#channel-with-bot` | карточка с саммари | ✅ 01.09 |
| 29 | дубли не плодятся, карточка освежается | четыре призыва в один тред | одна карточка, «из 5 сообщений» | ✅ 04.09 |
| 30 | статус, поставленный человеком, не затирается | освежить карточку в `В разборе` | статус на месте | ✅ 04.09 |
| 31 | квота модели не теряет обращение | выжечь 20 запросов Gemini | сообщение в `#duty-feed` со ссылкой | ✅ 01.09 |
| 32 | потерянное на квоте подтягивается позже | призвать в тот же тред после сброса квоты | данные из пропущенного сообщения в карточке | ✅ 04.09, `demo-4444, hub0` доехали |

## Комментарии к карточке

| # | Что проверяем | Как | Ожидаем | Итог |
|---|---|---|---|---|
| 33 | комментарий доходит до бота | написать в обсуждение карточки | событие `message` с телом | ✅ 04.09, канал `C0BU1P86005`, `channel_type: group` |
| 34 | комментарий связывается с карточкой | прочитать родителя треда | id карточки в payload | ✅ 04.09, `slack_list.list_record_id` |

Беседа списка — это его id с `F` на `C`. Родитель треда — сообщение Slackbot
с подтипом `list_record_comment` и текстом «A comment was added».

## Отметки в треде

| # | Что проверяем | Как | Ожидаем | Итог |
|---|---|---|---|---|
| 35 | шаг виден в Workflow Builder | искать в Search steps | находится по имени приложения или шага | ✅ 04.09, с суффиксом `(local)`; в списке слева **не показывается** |
| 36 | воркфлоу зовёт шаг | сменить статус | вызов за секунды | ✅ 04.09, 3.4 сек |
| 37 | воркфлоу видит правки от приложения | сменить статус через API | вызов приходит | ✅ 04.09 |
| 38 | фильтр значения в триггере ломает логику | триггер «стал равен В разборе» | зовётся только на вход в разбор | ❌ 04.09, 4 вызова все «В разборе», 5 переходов в другие статусы — ни одного вызова |
| 39 | в каком виде приходит статус | посмотреть лог вызова | подпись или значение | ✅ 04.09, **подпись** «В разборе», не `in_progress` |
| 40 | отметки сводятся к статусу | пройти все пять статусов | нужная стоит, лишние сняты | ✅ 04.09 |
| 41 | повторный вызов безопасен | позвать шаг дважды подряд | второй ничего не портит | ✅ 04.09, `already_reacted` и `no_reaction` считаются успехом |
| 42 | глаз у обращения не мигает | выбор варианта по `ts` | один тред — всегда один вариант | ✅ 04.09 |
| 43 | какие имена эмодзи существуют | `reactions.add` по кандидатам | валидные проходят | ✅ 04.09, 8 вариантов глаза; `detective`, `eye_in_speech_bubble`, `loading`, `spinner` — `invalid_name` |
| 44 | кастомные эмодзи не переносятся | `:eyeses:`, `:eye_see:`, `:lookbzz:` в песочнице | отсутствуют | ✅ 04.09, `invalid_name` на все три |

Проверка 38 — главные грабли этапа: условие на значение в триггере надо
оставлять пустым, иначе отметка ставится и не снимается никогда.

## Обход

| # | Что проверяем | Как | Ожидаем | Итог |
|---|---|---|---|---|
| 45 | призыв без карточки получает карточку | пустая доска, три призыва в канале | три карточки | ✅ 04.09, `новых 3, за 56.8с` |
| 46 | отметка садится на сообщение призыва | призыв ответом внутри чужого треда | реакция на сообщении призыва, не на корне | ✅ 04.09 |
| 47 | лишние отметки снимаются | статус ушёл из `В разборе` | отметка снята | ✅ 04.09, `отметок 2` — одна поставлена, одна снята |
| 48 | тред без тега подхватывается | дописать в тред без тега | «Проблема» пополнилась | ✅ 04.09, через 2.5 мин |
| 49 | проход без модели дешёвый | обход при неизменной доске | секунды | ✅ 04.09, 1.8 с на 3 карточки |
| 50 | набор глаз задействован весь | 8000 слаковских `ts` | все 8 вариантов | ❌ 04.09 → ✅ после правки |
| 51 | лимит `conversations.replies` | 8 вызовов подряд | без 429 | ✅ 04.09, `limit=200` не урезан |

Проверка 50 нашла настоящую ошибку: **слаковские `ts` всегда кончаются на 9**,
поэтому `int(ts) % 8` давал только нечётные остатки и половина набора была
недостижима. Заменено на хеш — стало 12.2…12.7% на вариант.

Проверка 51 важна для прода: с 29.05.2025 у новых приложений вне Marketplace
`conversations.replies` ограничен 1 запросом в минуту и `limit` до 15. На наше
приложение это не действует, но на проде надо перепроверить — иначе схема
«один запрос на карточку за проход» рассыпется.
| 52 | призыв из канала **без бота** доезжает | сообщение с тегом в `#channel-without-bot` `C0BV0EZESJC` | карточка на доске | ✅ 07.09, `новых 2` |
| 53 | тред оттуда читается | то же | саммари по содержанию треда | ✅ 07.09, user-токеном |
| 54 | отметка ставится и там | статус карточки → `В разборе` | реакция на сообщении призыва | ✅ 07.09, `eyeglasses` от `U0BTQ7D7B8F` |
| 55 | отметка от человека узнаётся своей | проход после неё | второй отметки не появляется | ✅ 07.09, `отметок 0` |

Проверки 52-55 закрывают второй путь: поиск user-токеном по всему воркспейсу.
Карточки оттуда **не второго сорта** — обратная связь автору работает, потому
что `reactions:write` в user-скоупах есть.
| 56 | интервал растёт с доской | `pace(15, N)` | 5 карточек → 15 с, 20 → 60 с, 100 → 300 с | ✅ 07.09 |
| 57 | фильтр поиска доезжает в запрос | `DUTY_SEARCH_FILTER=-in:#channel-without-bot` | совпадений меньше | ✅ 07.09, 11 → 8 |
| 58 | у поиска своя частота | лог за две минуты | раз в минуту, а не каждый проход | ✅ 07.09, 15:09:22 и 15:10:33 |

## Чего не проверяли

- поведение на **реальных** тредах: длинных, с curl-выводами, простынями ISIN
  и размытыми формулировками
- **замок на тред под нагрузкой**: код есть, но одновременного обращения
  события и обхода к одной карточке мы не воспроизводили
- надёжность воркфлоу: сколько изменений он теряет, если менять статус часто
- комментарии как канал связи: бот их слышит, но ничего с ними не делает
- что будет, когда карточек станет больше сотни: `items.list` берётся
  с `limit=100`, дальше нужна страничная выборка
- одновременные призывы в разных тредах
- поведение после перезапуска бота: пропущенное за простой не добирается
- права и поведение в рабочем воркспейсе — там другое приложение
