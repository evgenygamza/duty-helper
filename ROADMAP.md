# Бот дежурного — план

Чеклист по этапам, состояние на 10.09.2026. Архитектура и раскладка —
`README.md`, проверки и выясненное про API — `TESTS.md`, прод — `DEPLOY.md`.

## Э0 — репо и приложение под контролем ✅

- [x] `git init`, раскладка репозитория, ТЗ в `README.md`
- [x] Slack CLI v4.6.0, логин в песочнице `sandbox-d879184`
- [x] `slack init` в `bot/`, `slack app link` приложения `A0BSPKGE32Q`
- [x] манифест из репозитория: `bot/manifest.json`, `manifest.source: local`
- [x] `slack install --force`: бот 15 → 49 прав, user-токен 1 → 18

## Э1 — сбор ✅

- [x] каркас на `slack_bolt` + Socket Mode, точка входа `bot/app.py`
- [x] слушатель `@app.message` с регуляркой на тег группы
- [x] пересылка в канал очереди со ссылкой на исходное сообщение
- [x] документация Bolt подтянута в `temp/bolt-docs/`

**Выход:** призыв в известном канале появляется в ленте за секунды.
Проверено 31.08: призыв в `#channel-with-bot` доехал в `#duty-feed`,
такой же призыв в `#channel-without-bot` не доехал — событий оттуда нет.

## Э2 — саммари ✅

- [x] промпт `bot/prompts/summary.md`: три строки — суть, кто просит, данные
- [x] бот читает тред целиком и зовёт модель, выжимка уходит в ленту
- [ ] честная оценка: сколько раз саммари врёт на реальных тредах

**Выход:** в ленту приходит не сырой текст, а выжимка. Проверено 31.08.

## Э3 — доска

- [x] список Slack Lists «Очередь дежурного FactSet»: колонки под карточку
      плюс родные Completed / Assignee / Due Date от `todo_mode`
- [x] бот заводит карточку из обращения, статус `Новое`
- [x] промпт отдаёт JSON, а не прозу — доске нужны раздельные поля
- [x] доска видна команде: `slackLists.access.set` даёт каналу `write`
- [x] доска прибита закладкой в `#duty-feed`, а не теряется в ленте
- [x] **не затирать правки человека.** Обновление переписывает
      «Обращение», «Проблему» и «Данные» безусловно.
- [x] **срок на карточке.** `todo_mode` дал родную `Due Date`, но она
      пустая.
- [ ] **вьюха по умолчанию** — раскладка Board, группировка по статусу,
      закрытые отфильтрованы.
- [x] статусы согласованы: `Новое / В разборе / Ждём автора / Ждём FactSet /
      Закрыто` — каждый отвечает на «у кого ход»
- [x] убрать сообщение о новом обращении из ленты — тег группы
      в исходном треде уже пингует дежурного. Лента теперь только про ошибки

**Выход:** очередь дежурного живёт на доске. Проверено 01.09: призыв в
`#channel-with-bot` завёл карточку, статус переключается человеком из Slack.
Связь односторонняя — обратную забирает следующий этап.

## Долги — чинить, не дожидаясь этапа

- [x] **дубли.** Карточка живёт на тред, пока открыта: повторный тег её
      узнаёт и второй не заводит.
- [x] **молчаливые падения.** Любой отказ на призыве уходит в `#duty-feed`
      со ссылкой на тред

## Э4 — обратная связь от доски

- [x] отметки в треде по статусу карточки: нужная ставится, лишние снимаются
- [x] отметка садится **на сообщение призыва**, не на корень треда
- [x] ⏳ на оба «Ждём»: разница нужна на доске, а не в треде
- [x] вариант глаза выбирается один раз и дальше читается с сообщения
- [x] завели инцидент — сообщение в тред, сделано в Э6 обходом
- [x] не ловим: правки текста, `Assignee`, `Due Date` руками
- [x] `file_change` — Lists файловых событий не рождают, закрыто с контролем
- [x] **воркфлоу выброшен, решено 10.09**: секунды выигрыша против сборки
      кликами, молчаливой поломки и платных фич. Причины — `DEPLOY.md`
- [x] «В статусе с» штампует обход: в ячейке `статус@ts`, время ставится
      на расхождении. Точность «до прохода», порогам Э7 этого хватает
- [x] выкинуты `@app.function('react_in_thread')`, его регистрация
      в `collect.py` и мёртвые `ours()` с `reconcile()`; отметки ставит
      `apply()`, зовёт его обход
- [x] выкинуты из манифеста `functions`, `function_executed`
      и `function_runtime`. `org_deploy_enabled` **оставлен**: он нужен
      для установки на орг, а не только для шага. `file_change` из подписок
      убрали ещё раньше
- [x] воркфлоу в Workflow Builder оставлен лежать: сработать он не может,
      шага нет и триггер смотрит на удалённую доску. Ботом его не удалить,
      `admin.workflows.*` требует скоупа, которого у нас нет
- [ ] при переезде на прод подставить свои эмодзи, см. `DEPLOY.md`
- [x] песочница `duty-helper` `E0BU1D8791B`, приложение `A0BU7PW74C9`,
      CLI авторизован на уровне Organization
- [x] каналы `#duty-feed` `C0BU7RTJCDP`, `#channel-with-bot` `C0BU5TL051Q`,
      группа `S0BV0EVS7ME` — её заводит человек, боту `permission_denied`
- [x] `board.py` не хранит id колонок: схема читается на старте из `files.info`
- [x] id в `~/.config/duty-helper/duty-helper.env`, токены подставляет
      `slack run`
- [x] `#channel-without-bot` `C0BV0EZESJC` создан человеком: бот из канала
      выйти не умеет, а созданный ботом канал всегда с ботом внутри

**Выход:** автор обращения видит в своём треде, что карточку взяли и закрыли.

## Э5 — сверка

- [x] **отметки в треде против статуса карточки.** Без модели, реакции приходят
      вместе с тредом. Чинит пропуск вызова воркфлоу — известную дыру Э4
- [x] **карточка против треда**: последнее сообщение против «Прочитано».
      Ловит и сообщения **без тега** — на них событий не бывает вовсе
- [x] **обновить или отколоть сабтаск — решает модель.** Промпт смещён
      в сторону обновления: неверное дробление хуже широкой карточки
- [ ] проверить на живых тредах, как часто дробит зря
- [x] **призывы без карточки** по каналам с ботом; треды с ответами
      вскрываются отдельно, `conversations.history` даёт только верхний уровень
- [x] то же по каналам без бота — через поиск user-токеном, см. ниже
- [x] **просрочки** считаются по «В статусе с»; пороги и уведомления — Э7
- [x] **новое у вендора.** `issues.py moves <uuid>` за проход говорит, кто
      написал последним в обращениях, на которые смотрят карточки

### Поиск призывов

- [x] замерено 07.09: призывы идут из **19 разных каналов**, отсюда два
      равноправных пути, а не основной и костыль. Таблица — `TESTS.md`
- [ ] бота пригласить в ядро: свои каналы и оба `#factset-disasters-*`.
      Там события за секунды и всё работает ботовым токеном
- [x] **остальное добирает поиск user-токеном**: проверено 07.09 на канале
      без бота — карточка, тред и отметка
- [x] запрос `@<хэндл> after:<дата>`, хэндл **обязательно с `@`**
- [x] точка отсчёта не нужна: ищем в окне семи дней и сверяем со **всеми**
      карточками. Заглянуть глубже стоит длиннее запроса, но не дублей
- [x] **у поиска своя частота, раз в минуту**: индекс отстаёт секунд
      на двадцать, а запрос идёт под личным токеном
- [x] **что считать призывом** — вынесено в отдельный обходчик, см. ниже.
      Первая версия сверки ловит только тег группы регуляркой
- [x] **фильтр настраиваемый**, `DUTY_SEARCH_FILTER`, по умолчанию пустой
- [x] **user-токен в канале без бота делает всё**: находит, читает тред,
      ставит отметку
- [x] **`theirs()` узнаёт две личности** — иначе отметка от человека
      не считалась бы своей и обход повесил бы вторую поверх
- [ ] **чей это токен.** Он действует от лица человека, видит ровно то,
      что видит он, умирает вместе с его доступом, и весь поиск идёт под
      его именем.

### Обходчик призывов без тега

- [ ] кандидаты добываются дёшево — история, поиск, фильтры по каналам
      и авторам, окно по времени. Решает модель, и только по кандидатам
- [ ] **своя проверка до включения.** Ошибка модели мусорит на доске или
      молча теряет обращение; без способа померить точность включать нельзя
- [ ] решить, что делать с неуверенным решением: заводить карточку, спросить
      в `#duty-feed` или молчать

### Механика прохода

- [x] проверки **независимы**: каждая падает и отчитывается сама
- [x] **модель звать только если тред новее «Прочитано»**, а не по
      `updated_timestamp`: тот дёргается и от правки человека
- [x] **не больше трёх освежений за проход**: освежение это 15-20 с модели,
      чтение 0.1 с
- [x] **выдержка 10 минут на упавшее освежение**, иначе та же карточка
      перебирается каждый проход
- [x] запуск на старте и по таймеру. Старт заодно добирает за простой
- [x] флаг «уже идёт» — проход не накладывается на себя
- [x] свой канал отказов: сверка не расходится молча, а пишет в `#duty-feed`
- [x] отчёт одной строкой: карточек, отметок, новых, освежил, висит, за сколько
- [ ] запуск кнопкой или комментарием
- [x] **гонка разведена**: замок на тред, `duty_bot/locks.py`. Оба пути —
      событие и обход — идут через `cards.py`, и там же держится замок.
- [x] **интервал связан с числом карточек**: `max(DUTY_SWEEP_SECONDS, 3 ×
      карточек)`.
- [ ] **перепроверить лимиты на проде.** С 29.05.2025 у новых приложений
      вне Marketplace `conversations.replies` ограничен 1 запросом в
      минуту, а `limit` — 15 объектами.

**Выход:** пропущенное событие перестаёт быть потерянным навсегда, и призыв
из любого канала доезжает до доски.

## Э6 — действия дежурного

- [x] отметки в исходном треде по статусу карточки — сделано в Э4
- [x] **сообщение в тред про заведённый инцидент**, ровно один раз: память
      это сам тред
- [x] **«закрой <причина>»**: статус «Закрыто», причина дописывается
      в «Разбор» и уходит сообщением автору

### Переписка с FactSet из карточки

- [x] скрипты портала скопированы в `fs_issue_tracker/portal/`
- [x] промпты `letter_draft.md` и `incident_draft.md` рядом с `summary.md`
      и `repeat.md` — устройство то же
- [x] **автомат согласования в коде, а не в промпте**: «письмо», «инцидент»,
      «поправь ...», «отправляй» / «заводи»
- [x] **наружу по умолчанию ничего не уходит**: `DUTY_SEND_OUTWARD` выключен,
      и «отправляй» пишет письмо в файл вместо портала
- [x] **«отправляй» отвечает в привязанное обращение, «заводи» начинает
      новое**: продолжить разговор дешевле, чем начать
- [x] **маршрут нового issue** решает промпт, код сверяет со списком формы
      и выбрасывает выдуманное. Виден строкой «Куда», меняется «поправь»
- [~] **заведение инцидента в Jira** — перенесено в Э9: Rovo в демоне
      не живёт, а `mcp-atlassian` работает по токену и подключится вместе
      с клиентом MCP. Пока `incident.py` кладёт поля файлом
- [x] замок на карточку: два «отправляй» подряд = одно письмо
- [x] **грабли скилла закрыты кодом**: автоперелогин, локальный фильтр
      вместо `searchTerm`, «кто последний» по автору комментария, закрытые
      issue в выдаче `moves`
- [x] **сессия портала обновляется без человека**: три куки с разными
      часами, от часа до месяца

**Выход:** дежурный работает с доски, а не бегает по тредам, и переписка
с вендором не отстаёт от инцидента.

## Э7 — напоминания

- [x] в ячейке `статус@ts`, обход штампует время на расхождении — сделано
      10.09, проверки 87-90
- [x] `DUTY_USER_ID` из окружения перебивает всё: работает сегодня, без токенов
- [x] **`/duty` в канале**, `core/duty.py`: без аргумента отвечает, кто дежурит
      и что об этом думает `DUTY_USER_ID`; с `@именем` переписывает состав
      группы. Пишет сначала ботовым токеном, потом юзерским, и называет,
      каким получилось
- [x] **команда принимает нескольких**: `/duty @я @сменщик` кладёт в группу
      обоих. Напоминания пойдут всем, кто в группе: Slack не хранит порядок
      участников, поэтому «первый и есть настоящий» было бы гаданием
- [x] смена дежурства уходит строкой в `#duty-feed`, а вопрос «кто дежурит»
      остаётся у спросившего. Лента теперь не только про ошибки
- [x] **право на запись группы: пишем юзерским токеном**, решено 10.09.
      В user-скоупы добавлен `usergroups:write`, приложение переустановлено,
      запись проверена. Ботовым токеном по-прежнему `permission_denied` —
      приложению это можно только там, где управление группами открыто всем,
      и ослаблять настройку воркспейса мы не стали
- [x] переустановка выдала скоуп **тому же** токену: `SLACK_USER_TOKEN` в env
      менять не пришлось
- [ ] на проде проверить заново: писать группу сможет только человек, которому
      это разрешено в воркспейсе, а группу из IdP не правит и он — только SCIM
- [ ] то же самое, но раз в сутки из задачи `Duty on Sprint`, когда придёт
      токен Jira. Вызов тот же, добавится только источник имени
- [ ] состав группы призывов, `usergroups.users.list`. В песочнице там ровно
      один человек. **Открытый вопрос:** ротируется ли состав
      `@qa-factset-dutyman` в рабочем воркспейсе вместе с дежурством. Если да,
      это постоянный ответ, и Jira для Э7 не нужна вовсе
- [ ] Jira по запросу выше, когда появится токен
- [x] «сказано один раз» без своего состояния: память — история лички,
      карточка узнаётся по адресу треда без параметров
- [x] **призыв без 👀 полчаса** — `core/remind.py`, проверка `напомнил`
      в обходе. Пишет всем, кого отдаёт `whom()`. Проверки 101-107
- [x] **«ход наш» без движения сутки** — карточка в «В разборе». Оба «Ждём»
      сюда не входят: там ход не наш. Проверки 108-113
- [ ] инцидент без движения 5 дней — в канал
- [ ] у ФС обращение закрыто, а инцидент открыт — сразу в личку
- [x] **письмо без ответа от ФС 7 дней** — в канал, `#duty-feed`. Смотрим
      только карточки в «Ждём FactSet»: остальные ответа и не ждут.
      Проверки 116-120, живого случая в трекере пока не нашлось
- [x] **окна у вопроса к трекеру больше нет**: портал спрашивается по
      обращениям, которые названы карточками, а не по тем, что двигались
      за последнюю неделю. Ответ вендора, пришедший в простой бота,
      не теряется. Проверка 115
- [ ] сигнал «вендор спросил, а мы молчим» — на ITSM-72571 его отсутствие
      стоило шести дней

**Выход:** бот тормошит, а не только показывает.

## Э8 — разговор с моделью из карточки

- [ ] слушатель комментариев: событие из беседы списка → `thread_ts` →
      родитель → `slack_list.list_record_id` → карточка
- [ ] отсекать своё эхо, иначе бот ответит сам себе
- [ ] отвечать в тот же тред `chat.postMessage` с `thread_ts`
- [ ] **причесать команды.** Сейчас правило неявное: команда — первое
      слово комментария, поэтому «Напиши письмо» уходит в тишину,
      неотличимую от поломки.
- [ ] где виден ответ: комментарий, «Разбор» или и то и другое
- [ ] лимит длины ответа в ячейке — не проверялся

**Выход:** дежурный разговаривает с моделью с доски, не уходя в Slack-тред.

## Э9 — автоматический разбор

- [x] чем ходить: MCP это stdio-процессы, бот поднимает их сам — Postgres
      через `npx server-postgres` по ssh-туннелям, Jira `uvx mcp-atlassian`,
      gitlab `npx server-gitlab`. Confluence только REST с сервисным токеном,
      Rovo демону недоступен
- [ ] клиент MCP в боте: поднять stdio-процесс, получить список инструментов,
      звать их
- [ ] **белый список только на чтение.** У `mcp-atlassian` есть инструменты,
      которые правят задачи; модели они попадать не должны
- [ ] **заведение инцидента — названное исключение из этого списка.** Пишет
      его код по команде «заводи», а не модель по своему усмотрению
- [ ] **чей токен у бота.** Slack и FactSet уже переключаются через env
      (`SLACK_USER_TOKEN`, `FACTSET_LETTERS_HOME`), Jira добавит третий.
      Нужно решение про сервисные учётки и одно место, где видно,
      от чьего имени бот действует
- [ ] цикл function calling с потолком шагов: каждый шаг — вызов модели,
      без потолка расследование не кончится
- [ ] команда «расследуй» в комментариях рядом с «письмо» и «инцидент»,
      с теми же 👀 и ✅. Результат в колонку «Разбор» и строкой в тред
- [ ] первый заход на двух источниках: **Postgres стендов и UDF по HTTP**.
      С них дежурный начинает руками, оба доступны без единого нового
      токена, и оба проверяемы — ответ модели сверяется тем же запросом
- [ ] где граница: бот собирает факты, выводы делает человек
- [ ] чем это проверять — разбор, который врёт, хуже пустого. Это гейт:
      без способа померить точность включать нельзя

**Выход:** дежурный открывает карточку и видит не только жалобу,
но и то, что уже проверено за него.

## Э10 — прод

- [x] **точка отсчёта `DUTY_SINCE`**: дата, `30d` или пусто. Всё, что старше,
      обход не карточит; окно поиска считается от неё. Пустое значение
      означает «с этого запуска», и тогда простой теряется — на проде задавать
      датой
- [x] **режим `DUTY_DRY_RUN`**: читает всё, не пишет ничего, говорит в лог,
      что сделал бы. Модель не зовётся
- [x] права по факту вызовов собраны, список в `DEPLOY.md`

- [ ] набор прав для рабочего воркспейса собрать по факту использования,
      не по догадке. Прежние обоснования — `git show deb4881:bot/SLACK-APP.md`
- [ ] аппрув админа рабочего воркспейса
- [ ] контейнер, argocd, кубер — процесс без присмотра больше не годится
- [ ] ключ модели в секрете, не в файле у меня на маке
- [ ] **платный тариф с Lists** в рабочем воркспейсе: доска бота это Slack
      List, на бесплатном тарифе её нет вовсе. Листы у компании есть, а что
      приложению дадут `lists:read` и `lists:write`, проверяется установкой
- [ ] кто заводит доску и владеет ею: сейчас её создал бот, права раздавали
      руками через `slackLists.access.set`
- [ ] что делать с прежним скиллом дежурства — он остался у Гамзы локально
- [ ] вернуть Claude вместо Gemini; заодно посмотреть, ложится ли сюда kagent

**Выход:** работает без моего мака и переживает мой отпуск.

## Э11 — ИИ-настройка

- [ ] прогнать промпты по настоящим тредам и посчитать, где модель врёт
- [ ] отдельно померить решение про сабтаски: как часто дробит зря
      и как часто не дробит там, где надо
- [ ] сравнить варианты формулировок, а не править на глаз
- [ ] зафиксировать способ проверки, чтобы правку промпта можно было
      оценить, а не «вроде получше»
- [ ] посмотреть, ложится ли сюда kagent

**Выход:** правку промпта можно оценить числом, а не ощущением.

## Дальний бэклог

- [ ] **алерт и обращение — одно и то же или разное?** У обращения есть
      автор, которому нужен ответ, отметка в треде и статус «Ждём автора».
- [ ] **как не утонуть.** Один упавший сервис даёт десяток сообщений
      подряд, значит карточка на инцидент, а не на сообщение
- [ ] **нужны ли они на доске вообще**, или дежурному достаточно, что алерты
      висят в своих каналах
- [ ] **команда на настройку таймеров**: пороги напоминаний сейчас константы
      в `core/remind.py`, менять их можно только правкой кода

## Надо решить, а не сделать

- **статусы карточки.** Обсуждается. Предложение: `Новое / В разборе /
  Ждём ответа / Закрыто` плюс колонка «от кого ждём» и колонка «Инцидент».
  Спорные места: «Просроченное» — производная от времени, а не состояние;
  «Инцидент» — факт, а не позиция; и нужно ли различать, ждём мы автора
  или вендора. Гейт для прод-доски: в песочнице набор уже зашит,
  колонки задаются только при создании
- **владелец доски на проде.** Человек или бот
- ~~**колонка «Причина закрытия».**~~ Решено 09.09: причина пишется
  в «Разбор». Отдельной колонки не будет — добавить её можно только руками
  в интерфейсе, API колонок к готовому списку не добавляет
- **приватные каналы.** User-токен видит только то, где состоит человек.
  Обращения из чужих приватных каналов не увидит никто

## Чего пока не знаю

1. что реально приходит при смене статуса на доске
2. упрётся ли установка в рабочем воркспейсе в аппрув админа и надолго ли

## Хвосты

- дежурный **захардкожен** на Гамзу в скилле, `references/duty-channel.md`;
  чем заменить, разобрано в Э7
- доска ITSM 5660 **неполна**: ITSM-72571 на ней нет, хотя Team проставлен —
  инциденты искать поиском по JQL
- обкатанный промпт прежнего обхода —
  `git show deb4881:skills/factset-duty/scripts/sweep-prompt.txt`
