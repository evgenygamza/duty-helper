# Бот дежурного — план

Состояние на 31.08.2026. ТЗ и архитектура — в `README.md`.

Прежняя ветка на `claude -p` и `launchd` снята: агент выгружен, обход, промпт
и вотермарка удалены. Скилл дежурства выселен из репозитория в
`~/.claude/skills/factset-duty` и живёт рядом с остальными скиллами.

## Э0 — репо и приложение под контролем ✅

- [x] `git init`, раскладка репозитория, ТЗ в `README.md`
- [x] Slack CLI v4.6.0, логин в песочнице `sandbox-d879184`
- [x] `slack init` в `bot/`, `slack app link` приложения `A0BSPKGE32Q`
- [x] манифест из репозитория: `bot/manifest.json`, `manifest.source: local`
- [x] `slack install --force`: бот 15 → 49 прав, user-токен 1 → 18

**Выход достигнут:** правка прав = правка `bot/manifest.json` плюс одна команда.

## Э1 — сбор ✅

Бот слышит призывы в каналах, где состоит, и пересылает их в канал очереди.
Из остальных каналов Slack событий не шлёт — это забирает Э4.

- [x] каркас на `slack_bolt` + Socket Mode, точка входа `bot/app.py`
- [x] слушатель `@app.message` с регуляркой на тег группы
- [x] пересылка в канал очереди со ссылкой на исходное сообщение
- [x] документация Bolt подтянута в `temp/bolt-docs/`

**Выход:** призыв в известном канале появляется в ленте за секунды.
Проверено 31.08: призыв в `#channel-with-bot` доехал в `#duty-feed`,
такой же призыв в `#channel-without-bot` не доехал — событий оттуда нет.

## Э2 — саммари ✅

- [x] промпт `bot/prompts/summary.md`: три строки — суть, кто просит, данные
- [x] бот читает тред целиком и зовёт модель, выжимка уходит в ленту
- [ ] честная оценка: сколько раз саммари врёт на реальных тредах

**Выход:** в ленту приходит не сырой текст, а выжимка. Проверено 31.08.

**Модель временно не Claude.** У организации Anthropic нет кредитов:
`models.retrieve` отвечает `200`, а `messages.create` — `400 credit balance
is too low`. Пока работаем на бесплатном тире Gemini, `gemini-3.6-flash`.
Возврат на Claude — правка одного файла `bot/duty_bot/summarize.py`,
промпт и склейка треда не меняются.

## Э3 — доска

- [x] список Slack Lists «Очередь дежурного FactSet», `F0BTMKKEA31`:
      обращение, статус, кто просит, канал, тред, данные
- [x] бот заводит карточку из обращения, статус `Новое`
- [x] промпт отдаёт JSON, а не прозу — доске нужны раздельные поля
- [x] доска видна команде: `slackLists.access.set` даёт каналу `write`
- [x] доска прибита закладкой в `#duty-feed`, а не теряется в ленте
- [ ] **вьюха по умолчанию** — раскладка Board, группировка по статусу,
      закрытые отфильтрованы. API для вьюх нет (`slackLists.views.*` —
      `unknown_method`), сохраняется руками кнопкой `Save as New View`
- [ ] договориться о статусах и перестроить схему под них
- [ ] убрать сообщение о новом обращении из ленты — тег группы
      в исходном треде уже пингует дежурного, второй пинг лишний

**Выход:** очередь дежурного живёт на доске. Проверено 01.09: призыв в
`#channel-with-bot` завёл карточку, статус переключается человеком из Slack.
Связь односторонняя — обратную забирает следующий этап.

## Долги — чинить, не дожидаясь этапа

- [ ] **дубли.** Второй тег группы в том же треде заведёт вторую карточку.
      Правило: карточка живёт на тред, пока открыта. Повторный тег в живом
      треде обновляет её, в треде с закрытой карточкой — заводит новую.
      Ищем среди открытых по ссылке на тред, своего хранилища не заводим
- [ ] **молчаливые падения.** Если Gemini или Slack отвалятся, исключение
      уйдёт в лог, карточки не будет, и никто об этом не узнает. Ответ
      в `#duty-feed` «обращение не обработал, вот ссылка» — это же
      становится единственным поводом боту писать в канал

## Э4 — обратная связь от доски

Сначала договориться о статусах: их набор определяет всё остальное.
Событий Lists у Slack нет, поэтому обходов два — воркфлоу с триггером
«элемент изменился» и вебхуком, либо опрос `items.list` по таймеру.

- [ ] **обсудить статусы**: какие нужны, кто их ставит, что означает каждый
- [ ] выбрать механику и проверить её на песочнице
- [ ] бот видит смену статуса и знает, что с карточкой что-то произошло
- [ ] комментарии к элементу: у каждого свой тред, механика не проверена

**Выход:** доска перестаёт быть односторонней.

## Э5 — сбор отовсюду

Слушать можно только каналы, где бот состоит. Остальное добирает поиск.
Одна механика, три способа запуска.

- [ ] поиск user-токеном `@qa-factset-dutyman -in:<шумные> -from:<боты>
      after:<точка>`
- [ ] запуск: кнопкой «актуализировать призывы», на старте бота и по таймеру
- [ ] точка отсчёта: последняя виденная, чтобы добирать пропущенное за простой
- [ ] тюнинг: ловить все формы упоминания — личные меншены, треды без тега,
      ответы на призыв
- [ ] фильтры: отсев ботов и шумных каналов

**Выход:** призыв из любого канала доезжает за минуту, а простой бота
не теряет обращения.

## Э6 — действия дежурного

Всё здесь опирается на Э5: без обратной связи бот не знает, когда действовать.

- [ ] реакции в исходном треде: взяли в работу, завели инцидент
- [ ] сообщение в тред, когда по обращению заведён инцидент
- [ ] интеграция со скиллом `factset-letters`: письмо в FactSet из карточки
- [ ] приоритет по времени: чем дольше висит, тем выше
- [ ] закрытие карточки с причиной

**Выход:** дежурный работает с доски, а не бегает по тредам.

## Э6.5 — актуализация тредов

В тред пишут и после того, как карточка заведена: уточняют, приносят данные,
объявляют, что починилось. Сейчас карточка снимок первого мгновения.

- [ ] перечитывать тред и обновлять саммари карточки
- [ ] чем триггерить: новое сообщение в треде, кнопка, таймер
- [ ] не переписывать то, что человек поправил руками

**Выход:** карточка описывает обращение сейчас, а не как оно выглядело вначале.

## Э7 — напоминания

- [ ] уведомления в личку дежурному
- [ ] призыв без реакции 30 минут
- [ ] «наш ход» без движения сутки
- [ ] сигнал «вендор спросил, а мы молчим» — на ITSM-72571 его отсутствие
      стоило шести дней
- [ ] откуда берётся имя дежурного: сейчас захардкожено в скилле

**Выход:** бот тормошит, а не только показывает.

## Э8 — прод

- [ ] набор прав для рабочего воркспейса собрать по факту использования,
      не по догадке. Прежние обоснования — `git show deb4881:bot/SLACK-APP.md`
- [ ] аппрув админа рабочего воркспейса
- [ ] контейнер, argocd, кубер — процесс без присмотра больше не годится
- [ ] ключ модели в секрете, не в файле у меня на маке
- [ ] кто заводит доску и владеет ею: сейчас её создал бот, права раздавали
      руками через `slackLists.access.set`
- [ ] что делать с прежним скиллом дежурства — он остался у Гамзы локально
- [ ] вернуть Claude вместо Gemini; заодно посмотреть, ложится ли сюда kagent
- [ ] оценить, как часто саммари врёт — только на настоящих тредах.
      Отдельная идея: натаскать на этом скилл поверх дьюти-бота, с MCP

**Выход:** работает без моего мака и переживает мой отпуск.

## Надо решить, а не сделать

- **статусы карточки.** Обсуждается. Предложение: `Новое / В разборе /
  Ждём ответа / Закрыто` плюс колонка «от кого ждём» и колонка «Инцидент».
  Спорные места: «Просроченное» — производная от времени, а не состояние;
  «Инцидент» — факт, а не позиция; и нужно ли различать, ждём мы автора
  или вендора. Гейт для Э4
- **владелец доски на проде.** Человек или бот
- **приватные каналы.** User-токен видит только то, где состоит человек.
  Обращения из чужих приватных каналов не увидит никто

## Чего пока не знаю

1. как писать комментарий в тред элемента списка
2. что реально приходит при смене статуса на доске
3. упрётся ли установка в рабочем воркспейсе в аппрув админа и надолго ли

## Проверено в песочнице

Приложение `duty-helper`, бот `dutyhelper` (`U0BS4J84MMM`),
группа `@qa-factset-dutyman` = `S0BTF0KMJV6`.

Каналы: `#channel-with-bot` `C0BSKTQ1M5G`, `#channel-without-bot` `C0BTSTZ2J3X`,
`#duty-feed` `C0BTQL7AK4K`.

- поиск группы работает, если в запросе есть `@`: `@qa-factset-dutyman`
  находит настоящие теги `<!subteam^ID>`, без `@` — только текстовые вхождения
- user-токен читает тред в канале, где бота нет: `conversations.replies`
  отдаёт все сообщения, бот-токен там получает `not_in_channel`
- поиск находит призыв из канала без бота примерно через 20 секунд —
  индекс отстаёт, но незначительно
- доска прибивается вкладкой через `bookmarks.add` с `type=link`
- `channels:leave` у ботов не бывает: приложение не может выйти из канала.
  Канал без бота создаётся user-токеном с правом `channels:write`
- подписка на `app_mention` требует scope `app_mentions:read`, иначе
  `apps.manifest.validate` не пропускает манифест
- манифест берётся через хук `get-manifest` из `.slack/hooks.json`; хук по
  умолчанию ищет файл `manifest.json`
- работают `chat.update`, `chat.delete`, `reactions.add/remove`,
  `slackLists.create`, `items.create`, `items.list`, `items.info`
- ячейка в `initial_fields` адресуется через `column_id`, а не через `key` —
  отсюда `invalid_arguments` 25.08. `key` работает только на чтение
- `slackLists.info` не существует; схему списка отдаёт `files.info`,
  id списка это id файла
- список, созданный ботом, никому не виден: `channels: []`. Публикация
  ссылки в канал даёт доступ только на чтение. Права раздаёт
  `slackLists.access.set` — `access_level` `read`/`write`/`owner`,
  и либо `channel_ids`, либо `user_ids`, вместе нельзя
- у Gemini таймаут ставится на клиенте (`http_options`), не в вызове;
  `gemini-3.7-flash` на бесплатном тире перегружен, `3.6-flash` отвечает
- `assistant.search.context` → `invalid_action_token`: метод для ассистентов
  Slack, не наш путь

## Хвосты

- дежурный **захардкожен** на Гамзу в скилле, `references/duty-channel.md`
- доска ITSM 5660 **неполна**: ITSM-72571 на ней нет, хотя Team проставлен —
  инциденты искать поиском по JQL
- обкатанный промпт прежнего обхода —
  `git show deb4881:skills/factset-duty/scripts/sweep-prompt.txt`
