# Бот дежурного — план

Про живого бота в Slack: Bolt, Socket Mode, постоянный процесс.
Ветка на `claude -p` и `launchd` описана в `skills/factset-duty/ROADMAP.md`, она временная
затычка и живёт своей жизнью до Э6.

Состояние на 26.08.2026. ТЗ и архитектура — в `README.md`.

## Э0 — репо и приложение под контролем

- [x] `git init`, первый коммит
- [x] раскладка репозитория: `bot/`, `skills/`, `temp/`, ТЗ переехало в `README.md`
- [x] Slack CLI v4.6.0 поставлен в `~/.slack/bin/slack`
- [x] `slack login` в песочнице `sandbox-d879184`
- [x] `slack init` в `bot/` — `.slack/config.json` и `.slack/hooks.json` на месте
- [x] `slack app link`: приложение `A0BSPKGE32Q`, команда `T0BSDLTDKBM`,
      окружение `local`
- [x] манифест из репозитория: `bot/manifest.json`, `manifest.source: local`
- [ ] `slack install`, свежие токены в `~/.config/duty-helper/sandbox.env`

**Выход:** правка прав = правка файла плюс одна команда, веб-интерфейс не нужен.
Остался `slack install` — он перезапишет права живого приложения.

## Э1 — каркас бота

- [ ] `poetry`, `slack_bolt`, `SocketModeHandler` — всё внутри `bot/`
- [ ] конфиг: три токена, id группы призывов, id канала очереди
- [ ] обработчики `app_mention` и `reaction_added` пишут в лог

**Выход:** событие из Slack доезжает до кода локально.

## Э2 — сбор упоминаний

- [ ] поиск user-токеном `@qa-factset-dutyman -in:<шумные> -from:<боты>
      after:<точка>`, раз в минуту
- [ ] вотермарка и дедуп по `ts`, чтобы обращение не завелось дважды
- [ ] чтение треда целиком через `conversations.replies` user-токеном
- [ ] поправить запрос в `skills/factset-duty/references/slack-triage.md` и `SKILL.md`: потерян `@`

**Выход:** через минуту после призыва бот знает о нём и держит текст треда.

## Э3 — трекер

- [ ] `items.create`: элемент из обращения, саммари пока шаблонное
- [ ] схема `items.update` — `invalid_arguments` с 25.08, дело в ячейках
- [ ] комментарии к элементу: у каждого свой тред, механика не проверена
- [ ] что приходит при смене статуса: событий Lists нет, остаётся воркфлоу
      с вебхуком либо опрос `items.list`

**Выход:** упоминание само становится элементом, смену статуса бот видит.

## Э4 — саммари пишет ИИ

- [ ] выбрать чем: Claude API из бота, kagent в кубере или что-то третье
- [ ] промпт взять из `skills/factset-duty/scripts/sweep-prompt.txt`, он обкатан
- [ ] честная оценка: сколько раз саммари врёт на реальных тредах

**Выход:** карточку видно без открытия треда.

## Э5 — действия дежурного

- [ ] 🎫 → инцидент в ITSM, через `factset-letters`
- [ ] закрытие карточки с причиной свободным текстом
- [ ] напоминания: призыв без 👀 30 минут, «наш ход» без движения сутки
- [ ] сигнал «вендор спросил, а мы молчим» — на ITSM-72571 его отсутствие
      стоило шести дней

**Выход:** бот не только показывает очередь, но и тормошит по ней.

## Э6 — прод

- [ ] набор прав для рабочего воркспейса — собрать по факту использования,
      не по догадке. Прежние обоснования по каждому scope лежат в истории:
      `git show deb4881:bot/SLACK-APP.md`
- [ ] аппрув админа
- [ ] контейнер, argocd, кубер — как остальной стек
- [ ] выключить ветку на `claude -p`: `launchctl unload`

**Выход:** работает без моего мака.

## Чего пока не знаю

1. подхватит ли CLI приложение, созданное не им
2. упрётся ли установка в рабочем воркспейсе в аппрув админа и надолго ли
3. как писать комментарий в тред элемента списка
4. чем и где считать саммари в кубере
5. хватит ли user-токена на чтение тредов в приватных каналах, где человек
   не состоит — там не увидит ничего, и это дыра в сборе

Пятое проверить рано: если призывы летят в приватные каналы мимо человека,
схема с поиском теряет часть обращений.

## Проверено в песочнице

Приложение `duty-helper`, бот `dutyhelper` (`U0BS4J84MMM`), канал `C0BSKTQ1M5G`,
группа `@qa-factset-dutyman` = `S0BTF0KMJV6`.

26.08:

- оба токена живы, Socket Mode включён, app-токен отдаёт wss
- поиск группы работает, если в запросе есть `@`: `@qa-factset-dutyman`
  находит настоящие теги `<!subteam^ID>`, без `@` — только текстовые вхождения
- у бот-токена набор прав старый, приложение с новым манифестом не переустановлено
- Slack CLI v4.6.0 поставлен, `slack login` не пройден
- манифест не обязан лежать в корне и не обязан быть файлом: CLI берёт его
  через хук `get-manifest` из `.slack/hooks.json`, то есть путь произвольный
  и на его месте может стоять генератор — этим и закроем два контура

25.08, прогон `temp/sandbox_check.py` — 11 из 15:

- работают `chat.update`, `chat.delete`, `reactions.add/remove`,
  `slackLists.create`, `items.create`, `items.list`, `items.info`,
  `usergroups.list`, `chat.postMessage`
- `slackLists.items.update` → `invalid_arguments`: у элемента `fields: []`,
  надо добить схему ячеек
- `assistant.search.context` → `invalid_action_token`: метод для ассистентов
  Slack, не наш путь

## Хвосты

- дежурный **захардкожен** на Гамзу в
  `skills/factset-duty/references/duty-channel.md`
- доска ITSM 5660 **неполна**: ITSM-72571 на ней нет, хотя Team проставлен —
  инциденты искать поиском по JQL
- алерты Zabbix и дайджест отложены осознанно, см.
  `skills/factset-duty/BACKLOG.md`
- когда бот пишет меншен сам, ставить надо полную форму
  `<!subteam^ID|@handle>` — короткую поиск не увидит
